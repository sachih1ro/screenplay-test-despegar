package com.despegar.screenplay.utils;

import net.thucydides.core.webdriver.SerenityWebdriverManager;

import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;
import java.util.Set;

public class SearchAccommodationUtils {

    /*
    public static String getNewTabHandle(){
        Set<String> frames = SerenityWebdriverManager.inThisTestThread().getCurrentDriver()
                .getWindowHandles();
        SerenityWebdriverManager.inThisTestThread().getCurrentDriver().get

        String[] framesArray = frames.toArray(new String[frames.size()]);
        return framesArray[framesArray.length-1];
    }

     */

}
